package com.learmond.lpe_with_source

import android.app.Activity
import android.content.Context
import android.content.Intent
import androidx.annotation.NonNull
import io.flutter.embedding.engine.plugins.FlutterPlugin
import io.flutter.embedding.engine.plugins.activity.ActivityAware
import io.flutter.embedding.engine.plugins.activity.ActivityPluginBinding
import io.flutter.plugin.common.MethodCall
import io.flutter.plugin.common.MethodChannel
// This file was intentionally cleared to avoid duplicate Android plugin
// implementations. The single plugin used by this package is
// `LearmondNativePayWithSourcePlugin` in package
// `com.learmond.lpe_with_source` as declared in `pubspec.yaml`.

// (Original implementation removed.)
